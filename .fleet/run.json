{
    "configurations": [
        {
            "type": "command",
            "name": "Start built-in PHP server",
            "program": "php",
            "args": ["-S", "localhost:8000"]
        }
    ]
}